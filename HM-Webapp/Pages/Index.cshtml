@page 
@model IndexModel
@{
    ViewData["Title"] = "Home";
}
 <a href="/Index"><img alt="Hem" src="/img/morsan_small.png" width="300"></a>

<div class="container text-center">
  <!-- table headers -->
  <table class="table">
    <thead>
      <tr>
        <th>Namn</th>
        <th>Skicka SMS</th>
        <th>Födelsedag</th>
        <th>Mors-O-Meter</th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>

<!-- print persons from list -->
@foreach (var item in @Model.myPersons)
{
      <tr>
        <td>@item.Name</td>
        <td><button type="button" class="btn btn-info">@item.Phone</button></td>
        <td>@item.Birthday</td>
        <!-- FIXME: change to variable so background is responsive to counter -->
        <td class="bg-success"><h5>@item.Counter</h5></td>
        <td><button type="button" class="btn btn-info ">Ändra</button></td>
        <td><button type="button" class="btn btn-danger">X</button></a></td>
      </tr>     
}
    </tbody>
  </table>
</div>